一个Ｃ程序由一个或多个程序模块构成，每一个程序模块作为一个源程序文件。对于较大的程序，一般不希望把所有内容放在一个文件中，而是放在若干个文件里，由若干个源程序组成一个Ｃ程序，这样便于编写和编译，提高调试效率。一个源文件程序可以为多个Ｃ程序所共用。

所有的Ｃ头文件都在/usr/include中，可以通过Home查询。

在程序设计中，在划分模块时要求模块的“内聚性”强，与其他模块的“耦合性”弱。即模块的功能要单一，与其他模块的相互影响要尽量少。一般要求把Ｃ程序中的函数做成一个相对的封闭体，除了可以通过“实参－形参”的渠道与外界发生联系之外，没有其他渠道。这样的程序移植性好，可读性强。
